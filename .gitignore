# Editor backup files
*~
\#*\#
.#*

# Symlinks to home directory
.config

# Old configs (keep in separate location if needed)
slime/

# Nix build results
result
result-*

# Secrets (if any)
secrets/
*.secret
